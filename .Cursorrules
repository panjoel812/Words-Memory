# Words-Memory 项目规则

## 设计风格

1. **Apple Design风格**：所有界面元素都应遵循Apple设计语言，包括：
   - 简洁的界面
   - 优雅的排版
   - 合理的空白
   - 精细的动画过渡

2. **毛玻璃效果**：主要卡片和面板应使用毛玻璃磨砂效果，实现方式为：
   ```css
   .glass-effect {
     background: rgba(255, 255, 255, 0.7);
     backdrop-filter: blur(10px);
     -webkit-backdrop-filter: blur(10px);
     border-radius: 12px;
     border: 1px solid rgba(255, 255, 255, 0.18);
   }
   ```

3. **配色方案**：
   - 主题蓝：#007AFF
   - 背景色：#F5F5F7
   - 卡片背景：rgba(255, 255, 255, 0.7)
   - 文本主色：#333333
   - 文本次要色：#8E8E93

4. **单词卡片设计**：
   - 使用3D翻转效果展示单词正反面
   - 正面显示英文单词，背面显示中文释义选项
   - 卡片使用毛玻璃效果，保持界面一致性
   - 添加适当动画效果增强交互体验

## 命名规范

1. **CSS类名**：采用BEM命名方式
   ```
   .block__element--modifier
   ```

2. **JavaScript**：
   - 变量和函数：驼峰命名法（camelCase）
   - 类：帕斯卡命名法（PascalCase）
   - 常量：全大写下划线（UPPER_SNAKE_CASE）

3. **文件名**：
   - HTML：小写连字符（kebab-case）
   - CSS：小写连字符（kebab-case）
   - JS：小驼峰（camelCase）

## 代码组织

1. **HTML结构**：
   - 语义化标签优先
   - 清晰的嵌套结构
   - 明确的注释分区

2. **CSS组织**：
   - 全局样式
   - 组件样式
   - 媒体查询
   - 动画和过渡

3. **JavaScript模块**：
   - 核心应用逻辑
   - 单词处理模块
   - 记忆算法模块
   - UI交互模块

## 响应式设计规则

1. **断点设置**：
   - 移动端：< 768px
   - 平板端：768px - 1023px
   - 桌面端：≥ 1024px

2. **移动端优先**：
   - 基础样式为移动端设计
   - 使用媒体查询拓展到大屏幕

3. **导航适配**：
   - 移动端：底部导航栏
   - 桌面端：侧边导航栏

## 数据管理规则

1. **本地存储**：
   - 使用localStorage存储用户数据
   - 关键数据需进行备份提示

2. **数据结构**：
   - 单词数据
   - 学习进度
   - 用户设置
   - 记忆计划 

## 错误处理规则

1. **命令行错误**：
   - Windows PowerShell中不支持"&&"作为命令分隔符，需使用分号";"或单独执行命令
   - 在PowerShell中使用cd命令后跟路径时注意路径格式

2. **JavaScript错误**：
   - 在使用window.WordsMemoryWordBank前需确保该对象已被正确初始化
   - 在加载单词数据前添加错误捕获，避免因数据不存在导致界面崩溃
   - 使用try-catch包裹localStorage操作，处理配额或权限问题

3. **CSS兼容性错误**：
   - 对于backdrop-filter属性，需添加-webkit-前缀并提供降级样式
   - CSS 3D转换在低端设备上可能性能不佳，考虑提供简化替代方案 